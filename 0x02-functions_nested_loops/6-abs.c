#include "main.h"
/**
*_abs - get the absolute value
*
*@num: number to check
*
*Return: the value
*/
int _abs(int num)
{
	if (num < 0)
		return (-(num));
	else if (num > 0)
{
		return (num);
}
	else
	return (0);
}
